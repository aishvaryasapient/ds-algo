// Distance of nearest cell having 1 in a binary matrix from 0

const PositionCalculator = (arr)=>{
    let position=[];
    let m= arr.length, n=arr[0].length;
    
    return position;
}

export default PositionCalculator